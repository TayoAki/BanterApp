# marshmemos environment contract. Copy to apps/server/.env (server) and
# apps/mobile/.env (client). Never commit real values. Public EXPO_PUBLIC_*
# variables are bundled into the app and must never hold service-role, model,
# or webhook secrets.

# ---------------------------------------------------------------------------
# Client (Expo). Bundled into the app.
# ---------------------------------------------------------------------------
EXPO_PUBLIC_ENV=development
EXPO_PUBLIC_API_BASE_URL=http://localhost:8787
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=

# ---------------------------------------------------------------------------
# Server (API + worker). Server-only.
# ---------------------------------------------------------------------------
APP_ENV=development                # development | test | production
PORT=8787
DATABASE_URL=                      # postgres://... (Supabase direct/pooler connection)
DATABASE_POOL_MAX=10

AUTH_MODE=supabase                 # supabase | fixture (fixture only outside production)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=               # optional legacy HS256 secret; JWKS is used when absent
SUPABASE_JWT_ISSUER=               # optional override, defaults to ${SUPABASE_URL}/auth/v1

STORAGE_MODE=supabase              # supabase | local (local only outside production)
STORAGE_BUCKET_AUDIO=practice-audio
LOCAL_STORAGE_DIR=                 # required when STORAGE_MODE=local

PROVIDER_MODE=openai               # openai | fixture (fixture only outside production)
AI_API_KEY=
AI_BASE_URL=                       # optional
TRANSCRIPTION_MODEL=gpt-4o-transcribe
EVALUATION_MODEL=gpt-5
REWRITE_MODEL=gpt-5
VERIFIER_MODEL=gpt-5-mini
ROLEPLAY_MODEL=gpt-5-mini
TTS_MODEL=gpt-4o-mini-tts
TTS_VOICE=alloy
PROMPT_CONFIG_VERSION=prompts-v1

CONTENT_MANIFEST=production        # production | development (development publishes unreviewed drafts; refused in production)

BILLING_PROVIDER=none              # none | revenuecat
REVENUECAT_SECRET_API_KEY=
REVENUECAT_WEBHOOK_AUTH=           # value RevenueCat sends in the Authorization header
REVENUECAT_PRO_ENTITLEMENT_ID=pro
REVENUECAT_ENVIRONMENT=sandbox     # sandbox | production
ALLOW_DEMO_ENTITLEMENTS=false      # never true in production

FREE_SESSIONS_PER_UTC_DAY=1
PRO_SESSIONS_PER_UTC_DAY=10
RESERVATION_TTL_MINUTES=120
RAW_AUDIO_TTL_HOURS=24
TTS_TTL_HOURS=24
PLAYBACK_URL_TTL_SECONDS=300
UPLOAD_URL_TTL_SECONDS=600
MAX_TIMEZONE_CHANGES_PER_DAY=3

WORKER_ID=
WORKER_LEASE_SECONDS=120
WORKER_POLL_MS=1000
TIMEOUT_TRANSCRIBE_MS=60000
TIMEOUT_EVALUATE_MS=30000
TIMEOUT_REWRITE_MS=30000
TIMEOUT_VERIFY_MS=30000
TIMEOUT_TTS_MS=45000

LOG_LEVEL=info
MONITORING_DSN=
